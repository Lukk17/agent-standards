[project]
name = "agent-standards-tools"
version = "0.1.0"
description = "Generator for agent-standards subagent distribution"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[project.scripts]
gen-subagents = "gen_subagents:main"

[tool.uv]
package = false
