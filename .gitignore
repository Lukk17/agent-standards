### OS & System Files
.DS_Store
Thumbs.db
*~
*.swp
*.bak

### IDEs & Editors
.vscode/*
!.vscode/mcp.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

### Environment & Secrets
.env
.env.*
!.env.example

### Claude Code Local Overrides
# These files contain your local API tokens, sandbox configs, and personal memory.
# They must never be tracked in the central repository.
.claude/settings.local.json
CLAUDE.local.md
.claude/CLAUDE.local.md

### General Local Overrides
# Catch-all for any other agent's local preference files you might add later.
*.local.json
*.local.md

### MCP & agent configs
# The MCP and hook configuration files ship as real committed files, because they
# hold no secrets: tokens come from environment variables. Keep any local copy
# that inlines a secret out of version control by giving it a .local.json name,
# which the rule above already ignores.

### Conversion Script Artifacts
# If you run the conversion scripts locally inside this repo,
# you might want to ignore temporary build or output folders if they are generated.
node_modules/
__pycache__/

### Python build artifacts
# Produced by `pip install ./tools`. Never commit.
tools/*.egg-info/
tools/build/
tools/dist/